apiVersion: v1
kind: Service
metadata:
  name: wordpress
  labels:
    app: wordpress
spec:
  selector:
    app: wordpress
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 5050
    targetPort: 5050
    nodePort: 5050
